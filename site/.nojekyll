# This file tells GitHub Pages not to process the site with Jekyll.
# Required for custom static sites (especially with subdirectories or non-Jekyll content) to avoid 404s or broken files.
# The deploy workflow will include it at the root of the published site.