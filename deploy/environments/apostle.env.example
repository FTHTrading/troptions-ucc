# Apostle / UnyKorn Sovereign Control Plane (chain 7332) — Environment
# Copy this file, fill real values, and pass to scripts/deploy-apostle.ps1
# NEVER commit the filled version. Use sovereign-control-plane secret loading patterns where available.

APOSTLE_CHAIN_ID=7332
APOSTLE_RPC_URL=https://your-apostle-rpc.example.com   # <--- REQUIRED (from controlled operator config or apostle-chain)
APOSTLE_PRIVATE_KEY=0x...                               # <--- REQUIRED for broadcast (deployer EOA). Rotate after Safe transfer.
# APOSTLE_ETHERSCAN_API_KEY=...                         # Optional, for forge verify if Apostle explorer supports it (or Blockscout key)

# Optional: gas / tx settings
# APOSTLE_GAS_LIMIT=...
# APOSTLE_GAS_PRICE=...

# Notes for this deployment (700M NST pledge collateral)
# - First target: DocumentHashRegistry + TroptionsReserveRegistry for the NST 700M USD cash pledge
#   (Troptions = secured party, Newpoint Statutory Trust = pledgor, DE reg 6985669, Scotia Bank Canada custody)
# - After deployment: immediately run the SAFE-ADMIN-TRANSFER-CHECKLIST and move owner to the production Safe.
# - Use the backend hasher against the exact OneDrive PDFs to obtain the content hashes for the first registerDocument + attestReserve calls.