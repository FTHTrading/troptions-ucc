# Base (chain 8453) — Mirror Environment (external EVM rail)
# For mirrored reserve + document-hash registries (not the canonical authority).
# Same reserve schema as Apostle. Governed by a separate Safe.

BASE_CHAIN_ID=8453
BASE_RPC_URL=https://mainnet.base.org
BASE_PRIVATE_KEY=0x...                                  # Deployer EOA (transfer to Safe immediately after)
# BASE_ETHERSCAN_API_KEY=...                            # For forge verify (BaseScan)

# Notes
# - Deploy identical contracts (DocumentHashRegistry + TroptionsReserveRegistry).
# - Admin to a Base Safe.
# - Good for app integrations, broader EVM ecosystem reach, and mirrored attestations.
# - Policy and the 700M NST pledge reserve object remain on the Apostle / UnyKorn sovereign control plane.